# Time Library v1.0.0

The time Vex library allows users to get formatted time, including Hour-Minute-Second and Day-Month-Year.

```
addlib time as time

globalvar currentTime = time.hms()     # using hms to get the Hour-Minute-Second
globalvar currentDate = time.dmy()     # using dmy to get the Day-Month-Year
```
